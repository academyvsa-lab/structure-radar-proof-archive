# System Proof Archive

Public archive of weekly system performance reports (UTC).

- Resolved trades: Target Hits + Stop Hits.
- Terminal events include Expired.
- Snapshot JSON files are machine-generated and versioned weekly.
